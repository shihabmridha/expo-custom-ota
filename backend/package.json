{
  "name": "@ota/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"Use `bun run dev` or `bun run dev:api` from the repository root.\" && exit 1",
    "start": "bun src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@expo/code-signing-certificates": "^0.0.6",
    "@hono/zod-validator": "^0.9.0",
    "@ota/contracts": "workspace:*",
    "@ota/db": "workspace:*",
    "@ota/protocol": "workspace:*",
    "@ota/types": "workspace:*",
    "fflate": "^0.8.3",
    "hono": "^4.13.1",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  }
}
